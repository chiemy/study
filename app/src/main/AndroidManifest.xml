<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chiemy.demo.study">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CustomRippleDrawableActivity"
            android:label="@string/ripple" />
        <activity
            android:name=".XfermodeTest"
            android:label="@string/xfermode" />
        <activity
            android:name=".ShaderTest"
            android:label="@string/shader" />
        <activity
            android:name=".ReflectionTest"
            android:label="@string/reflection" />
        <activity
            android:name=".MeshTest"
            android:label="@string/mesh"/>
    </application>

</manifest>